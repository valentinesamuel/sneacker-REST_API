// import {Product} from '../model/product.model';

const fetchAllMensShoes = async () => {
	// const products = await Product.find({});
	// return products;
	console.log('About to enter db to get all mens shoes');
};

export {fetchAllMensShoes};
